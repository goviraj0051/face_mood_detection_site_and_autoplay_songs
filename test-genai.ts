import { GoogleGenAI } from "@google/genai"; import dotenv from "dotenv"; dotenv.config(); const ai = new GoogleGenAI({apiKey: process.env.GEMINI_API_KEY || ""}); async function main(){ try { const res = await ai.models.list(); const arr = []; for await (const m of res) { arr.push(m.name); } console.log(JSON.stringify(arr)); } catch (e: any) { console.error("Error", e); } } main();
